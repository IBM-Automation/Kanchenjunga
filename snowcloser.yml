---
- hosts: localhost
  gather_fatcs: false
  tasks:
  
  
     - pip:
          name:
            - pysnow
            - requests
          extra_args: --user
          executable: pip3
    #     delegate_to: localhost

      - name: Closing the Snow incident
        snow_record:
          state: present
          table: change_request
          username: "admin"
          password: "Zh*-h92XnKkW"
          instance: "dev115228"
          number: "{{ incident_num }}"
          data:
            state: -3
            close_notes: "Resolved"
        register: incident
    #     delegate_to: localhost

      - debug: 
          var: incident.record.state
